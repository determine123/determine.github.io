# 演示媒体

当前主页使用 CSS 抽象图作为无媒体时的安全占位。以后可以将真实视频放到此目录，并在 `_pages/about.md` 中替换为 `<video controls poster="...">`。

建议文件名：

- `agent-workflow.mp4`
- `robotics-control.mp4`
- `agent-workflow.poster.jpg`

GitHub Pages 适合发布压缩后的短视频，建议单个文件控制在 25 MB 左右。
